[package]
name = "ferriswatch"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Shared themes and colour palettes for consistent styling across applications"
readme = "README.md"
repository = "https://onedev.hankin.io/rust/ferriswatch"
keywords = ["theme", "palette", "colors", "styling"]
categories = ["gui"]

[dependencies]
miette = { version = "7.6", features = ["fancy", "serde"] }
serde = "1.0"
thiserror = "2.0"
dioxus = { version = "0.7.10", optional = true }

[dev-dependencies]
rstest = "0.27.0"

[features]
dioxus = ["dep:dioxus", "dioxus/web"]
